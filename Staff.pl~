#!/usr/bin/perl
package Staff;
use Moose;

has '_id' => (is => 'rw', isa => 'Int');
has '_username' => (is =>'rw', isa => 'Str');
has '_name' => (is => 'rw', isa => 'Str');
has '_bookings' => (is=>'rw', isa => 'BookingManager');

sub check_clashes{}
sub add_booking{}
sub remove_booking{}
